#include <YSI\y_hooks>

#define DISCORD_CHANNEL_MAIN "1058543663485042688"

static
    DCC_Channel:channelMain;

hook OnGameModeInit() {
    /* DCC_SetBotActivity("Scavenge & Survive");

    channelMain = DCC_FindChannelById(DISCORD_CHANNEL_MAIN);

    DCC_SendChannelMessage(channelMain, "Servidor iniciado. Vem pegar o seu loot!"); */
}